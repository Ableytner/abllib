[pytest]
addopts=--tb short
        --color=yes
        --show-capture all
log_format = [%(levelname)s] %(filename)s @%(lineno)d: %(message)s
;log_date_format=%Y/%m/%d %H:%M:%S
log_cli = true
log_cli_level = DEBUG
